SpiceSage Website/App

1. Navigate to the frontend (root file):
   cd SpiceSage
   
2. Run the app using npm:
    npm start

3. The app will by default be at <http://localhost:3000>
   Although it should open automatically with npm.
